# Android verification

Android was not changed by this remediation. Existing scope remains the single `app-companion`
module (`pk.vexel.pgrcompanion`). Prior release evidence is retained in the existing Android
verification documents; no Play Console upload or signing-material operation was performed.
