# 12 — Bug / Tech Debt Register

## Web

Scope: Next.js frontend (`frontend/`).

| ID | Priority | Finding | Evidence |
|---|---|---|---|
| WEB-1 | **P2** | `docs/contracts/ROUTES.md` — the file CLAUDE.md designates as the authoritative, pilot-frozen route contract — documents the superseded PG/Supervisor/Admin route model (`/login`, `/register`, `/dashboard/pg/*` as "stable role areas") rather than the current clean-room 4-role route set. A reader treating this contract as authoritative (as instructed) will orient around routes that are now redirect-only stubs. | `docs/contracts/ROUTES.md:5-30` vs `frontend/app/dashboard/pg/*/page.tsx` (5-6 line redirect stubs) and `docs/CANONICAL_ROUTE_MAP.md` |
| WEB-2 | **P2** | `docs/CANONICAL_ROUTE_MAP.md` is itself incomplete/stale — missing ~15 currently-live, non-stub routes (`/academics/leave-requests`, `/academics/logbook`, `/academics/evaluations`, `/academics/monitoring`, `/academics/my-progress`, `/academics/reports/*` (5 routes), `/academics/supervisor-workload`, `/academics/workflow-data-quality`, `/academics/workflow-overview`, `/residents/document-requirements`, `/admin/pending-supervisor-links`, `/profile`, `/unauthorized`). Needs regeneration from the live `frontend/app/` tree. | `find frontend/app -maxdepth 2 -type d` vs `docs/CANONICAL_ROUTE_MAP.md` |
| WEB-3 | **P2** | Two parallel, both-tested, both-non-stub change-password surfaces exist (`/change-password` and `/dashboard/change-password`), not consolidated. Low risk (both work) but duplicated UI/maintenance surface with no documented reason for the split. | `frontend/app/change-password/page.tsx`, `frontend/app/dashboard/change-password/page.tsx`, both with passing `page.test.tsx` |
| WEB-4 | **P3** | Page-level component tests are missing for several newer, non-trivial domains even though their API-client layers are unit-tested: rotation-assignments, leave-requests, logbook, evaluations, supervision (assignments/import/data-quality), reports (5 sub-routes), profile, monitoring/workflow-overview/workflow-data-quality (3 of 4 review-adjacent routes untested; only `review-queue` has a page test). No functional defect found — `npm run build`/`typecheck`/`lint` all pass clean — this is a coverage gap, not a bug. | `frontend/app/academics/*/page.test.tsx` presence check; `lib/api/rotations.test.ts` and `lib/api/leave.test.ts` exist at client layer only |
| WEB-5 | **P3** | `backend/sims/academics/` has no dedicated `urls.py` despite `academics` being one of the largest active apps (training-records, periods, rotation-templates, evaluation-templates, logbook-categories, review-queue, data-quality all route through `/academics/*` in the web). Route registration location for this app should be confirmed in the API contract matrix (Fork C) — not a defect by itself, but worth resolving so the "one file per backend app" API-client convention in CLAUDE.md has a clear backend-side counterpart to point at. | `find backend/sims/academics -iname "urls*.py"` → no results |
| WEB-6 | **P3** | 20 npm-audit advisories (1 critical, 14 high) surfaced on fresh `npm ci` — not triaged for exploitability/reachability in this pass. | `npm ci` output, 2026-09-09 |
| WEB-7 | **P1** | `/academics/logbook*` and `/academics/evaluations*` — full, functioning draft/submit/review/verify workflows (4 pages each, real API wiring in `lib/api/academics.ts`) — are **absent from `frontend/lib/navRegistry.ts`** for every role (ADMIN/RESIDENT/SUPERVISOR/SUPPORT_STAFF; full 129-line file read confirms no entry) and are not linked from any `app/dashboard/{resident,supervisor,utrmc}/page.tsx`. A real user in any role has no in-app way to discover or reach these routes — only a typed/bookmarked URL works. Same unreachability applies to `/academics/my-progress`, `/academics/monitoring`, `/academics/supervisor-workload`, `/academics/reports/*` (5 routes), `/academics/workflow-overview`, `/academics/workflow-data-quality`. Only `rotation-assignments` and `leave-requests` (added same era, per `CLAUDE.md`'s "frontend as of 2026-07-25" note) made it into nav. This upgrades the "coverage gap" framing of WEB-4 to a functional break for Logbook/Evaluations specifically — they are not merely under-tested, they are unreachable in the shipped product. | `frontend/lib/navRegistry.ts` (full read, no `academics/logbook`\|`evaluations`\|`my-progress`\|`monitoring`\|`supervisor-workload`\|`reports`\|`workflow-` match); `grep -rn "academics/logbook\|academics/evaluations\|..." app/dashboard components/layout` → zero hits |
| WEB-8 | **P3** | `frontend/app/register/page.tsx` (265 lines, full self-registration form + `authApi.register()` wiring) is an orphan page with zero inbound links anywhere in `app/` and contradicts the "`/users/new` is the universal identity creation center" model in `CLAUDE.md`/`AGENTS.md`. Backend `register_view` (`backend/sims/users/api_views.py:94`) is gated by `ENABLE_PUBLIC_REGISTRATION`, default `False` (`backend/sims_project/settings.py:755-756`), so there is no live security exposure in the default deployment — but the frontend page ships regardless and is dead weight/confusion relative to the documented identity model. | `frontend/app/register/page.tsx`; `grep -rn "href=\"/register\"" app` → no hits outside `app/register`; `backend/sims_project/settings.py:755-756` |
| WEB-9 | **P3** | Logbook status display bypasses both of the codebase's status-label helpers and renders the raw backend enum directly: `app/academics/logbook/page.tsx:79-93` inline-checks `entry.status === 'VERIFIED'`/`'SUBMITTED'`/etc. and prints the raw string, so a verified entry shows literally "VERIFIED" instead of the terminology-locked **"Approved"** (`docs/contracts/TERMINOLOGY.md`: "Approved: supervisor verifies entry"). Separately, `lib/ui/status.ts` (lowercase-keyed: `pending`→"Submitted", `approved`→"Approved", etc., correctly used by Rotations/Leave Requests via `WorkflowStatusBadge`) and `components/ui/WorkflowStatusBadge.tsx`'s own hardcoded `STATUS_COLORS` (uppercase-keyed, no label remapping — just `formatStatusLabel` title-casing) are two independent, not-quite-reconciled status-label systems; no observed mislabeling in the routes that use `WorkflowStatusBadge` today, but the duplication plus Logbook's third, ad-hoc inline approach is latent-defect risk and confirms `07_CONTRACT_DRIFT_REPORT.md` CD-4's finding that Logbook was built against its own vocabulary never reconciled with the terminology lock. | `frontend/app/academics/logbook/page.tsx:79-93`; `frontend/lib/ui/status.ts`; `frontend/components/ui/WorkflowStatusBadge.tsx`; `backend/sims/academics/models.py:598-608` (real `LogbookEntry.status` choices: `DRAFT/SUBMITTED/VERIFIED/RETURNED/REJECTED/CANCELLED`) |

One P1 web finding (WEB-7): Logbook and Evaluations workflows are fully built but unreachable via
navigation. No P0 findings. No failing test/build/lint/typecheck in this pass (see
`10_TEST_AND_CI_BASELINE.md` "Web Tests & CI": lint/typecheck/test/build all pass clean) — WEB-7 is
a navigation-integration break, not a code-quality or build failure.

## Android

Scope: PGR Companion Android app only (`android/app-portal`, canonical). Classified P0 (drop
everything) – P3 (low priority polish).

| ID | Priority | Finding | Evidence |
|---|---|---|---|
| AND-1 | **P1** | `docs/ANDROID_PLAY_STORE_UPLOAD_CHECKLIST.md` was not updated for the current `1.1.4`/code-4 release — it still describes `1.1.3`/code-3 as "the release candidate" and its checklist items (store listing, Data Safety, reviewer access, screenshots) are all marked done for `1.1.3` only. Whoever uploads the already-signed `1.1.4` AAB to Play Console has no `1.1.4`-specific checklist, risking an outdated screenshot set, stale Data Safety answers, or the wrong versionCode confirmation. | `docs/ANDROID_PLAY_STORE_UPLOAD_CHECKLIST.md:14-51` vs `ANDROID_RELEASE_1.1.4.md` (records a newer, already-built-and-signed release) |
| AND-2 | **P2** | `android/README.md`'s module table states `app-portal` is `1.1.3`/code `3` — stale by one release. Low risk of active harm (build.gradle.kts is the actual source of truth and is correct) but will mislead anyone reading the README to orient themselves. | `android/README.md:7` vs `android/app-portal/build.gradle.kts:22-23` (versionCode 4 / "1.1.4") |
| AND-3 | **P2** | No `ViewModel` classes and no `rememberSaveable` for core screen state (`snapshot`, `state`, `error`, `notice`, `busy` in `InstitutionalWorkspace`) — all held via plain Compose `remember`. This survives recomposition but **not** process death (Android reclaiming a backgrounded app under memory pressure), unlike `rememberSaveable` or a ViewModel-backed `SavedStateHandle`. A resident who is backgrounded long enough to be process-killed and returns will see a full reload rather than restored UI state (data itself is safe — tokens persist — but mid-edit form state, scroll position, and the selected tab are not). Not yet reported as a user-facing bug, but a known architectural gap as more screens are added. | `android/app-portal/src/main/java/pk/vexel/pgrportal/InstitutionalScreen.kt` (`InstitutionalWorkspace`, grep confirms zero `ViewModel` matches under `app-portal/src`) |
| AND-4 | **P2** | Live "successful upload" path (a `200` with a stored file via `POST /api/resident-documents/{id}/upload/`) has never been executed against production — only non-mutating rejection probes were run, because there is no staging resident account to safely mutate. This is the single largest unverified surface in an otherwise thoroughly-verified release. | `ANDROID_RELEASE_VERIFICATION.md` "Not verified" section; `PGR_SIMS_ANDROID_API_INTEGRATION.md` "Live probes" table |
| AND-5 | **P3** | Lint: `private fun detailLines(...)` in `ResidentWorkflowScreens.kt:285` violates Compose naming convention (`ComposableNaming`) — cosmetic only, does not affect behavior. | `android/app-portal/build/reports/lint-results-debug.txt` |
| AND-6 | **P3** | 9-10 unused color resources in `res/values/colors.xml` (a full Material-ish palette: `primary`, `secondary`, `background`, `surface`, status colors) never referenced from Compose (the app uses `MaterialTheme`/`PgrTheme` tokens instead) plus an unnecessary `mipmap-anydpi-v26` folder given `minSdk=26`. Dead resource weight, no functional impact. | `android/app-portal/build/reports/lint-results-debug.txt`; `res/values/colors.xml` |
| AND-7 | **P3** | `android/app-companion` remains included in `settings.gradle.kts` and still builds, but is frozen/historical with no CI coverage and only a 2-line commit touching it in the last active period. Fine as intentional provenance-preservation per existing docs, but worth an explicit "when do we actually drop this module" decision at some point rather than leaving it indefinitely dual-included. | `android/settings.gradle.kts`; `git log -1 -- android/app-companion` → `20e709e` (2-line diff); no `app-companion` CI job in `.github/workflows/pgsims_drift_gates.yml` |

No P0 findings. No evidence of Vexel MedSIMS contamination anywhere in `android/`, `docs/`, or
repo-root `*.md` (verified by grep for "medsims", case-insensitive, zero hits) — flagged as
explicitly checked and clean, not merely assumed absent.

## Cross-cutting / Production / Security

Scope: contract inventory/drift, production infrastructure (read-only via `ssh test`), CI gates,
security/RBAC spot review. Full detail in `06_PLATFORM_API_CONTRACT_MATRIX.md`,
`07_CONTRACT_DRIFT_REPORT.md`, `09_PRODUCTION_BASELINE.md`, `10_TEST_AND_CI_BASELINE.md` "CI/Quality
Gates", `11_SECURITY_RBAC_REVIEW.md` — this section lists only items not already itemized there, plus
one new finding not previously reported.

| ID | Priority | Finding | Evidence |
|---|---|---|---|
| XC-1 | **P1 (new)** | A real production `.env`-style file — `docs/ARCHIVE/_pilot_cleanup/20260403T124127Z/backups/.env.active` — is **tracked in git and present at current HEAD** (not just history), containing a live-looking `SECRET_KEY` (76 chars) and `DB_PASSWORD` (32 chars) plus `ALLOWED_HOSTS`/`CSRF_TRUSTED_ORIGINS`/`CORS_ALLOWED_ORIGINS` naming real production hostnames (`pgsims.alshifalab.pk`, `pgsims.pmc.edu.pk`, `api.pgsims.alshifalab.pk`, an IP `34.29.33.138`). This is categorically different from the `.env.*.example` files elsewhere in the repo (those contain only placeholder values like `your-secret-key-here-...`, confirmed by inspection) — this one carries real-shaped secret material. Last touched by commit `3a507b3` ("Revert \"update\"", 2026-09-02). Do not print or re-derive the values; treat both `SECRET_KEY` and `DB_PASSWORD` as compromised and rotate them if they are still live anywhere (current prod `.env` on `vps-clone` was not compared value-for-value against this file in this pass — only presence/absence of env vars was checked per `09_PRODUCTION_BASELINE.md`'s "no secret values printed" rule — so live-match is unconfirmed, but the safe assumption is rotate-don't-verify). Recommend: `git filter-repo`/history scrub or at minimum immediate rotation of any matching live credential, then delete the file from the tree (it serves no archival purpose that requires keeping real secrets). | `git log -1 -- docs/ARCHIVE/_pilot_cleanup/20260403T124127Z/backups/.env.active` → `3a507b3`; `git cat-file -e HEAD:<path>` confirms still present at HEAD; file has 8 lines, `SECRET_KEY=` and `DB_PASSWORD=` both non-placeholder length |
| XC-2 | **P3** | The same leaked file's `ALLOWED_HOSTS`/`CSRF_TRUSTED_ORIGINS` list `pgsims.pmc.edu.pk` as a production hostname, but the **live** `/etc/caddy/Caddyfile` on `vps-clone` (per `09_PRODUCTION_BASELINE.md`) serves `pgsims.alshifalab.pk`, `pg.fmu.edu.pk`, and API-only `api.pgsims.alshifalab.pk`/`api.pgsims.pmc.edu.pk`/`android.pgsims.alshifalab.pk` — `pgsims.pmc.edu.pk` (the bare, non-`api.` host) is absent from the current Caddy config even though `docker/.env.staging.example` and this archived file both reference it. Likely just a decommissioned/renamed domain from the 2026-04-03 pilot-cleanup era; not a security issue, but confirms the archived `.env.active` reflects a superseded deployment topology, not the current one — another reason it's safe to delete rather than "keep for reference." | `09_PRODUCTION_BASELINE.md` Caddy section vs `docs/ARCHIVE/_pilot_cleanup/20260403T124127Z/backups/.env.active` line 4 |
| XC-3 | **P2 (see `10_TEST_AND_CI_BASELINE.md`)** | `scripts/check_brick_6_masters_directory_data_quality.sh` and `scripts/check_brick_8_academic_workflow_foundation.sh` hardcode the production host's absolute path (`ROOT="/home/munaim/srv/apps/pgsims"`) instead of the repo-relative pattern every other gate script uses — unusable outside that one host, and not wired into any CI workflow, so brick 6/8 gates have zero automated coverage today. | `scripts/check_brick_6_masters_directory_data_quality.sh:4`, `scripts/check_brick_8_academic_workflow_foundation.sh:4` |

No P0 findings in the cross-cutting scope beyond XC-1 (rated P1 rather than P0 because exploitability
against the *current* live production credential is unconfirmed — it may already have been rotated
since the 2026-04-03/2026-09-02 commits — but the committed material itself is a real, present-tense
exposure that should not wait for a future sprint). Production itself is healthy, in sync with
`main` modulo one docs-only commit, and cleanly migrated — see `09_PRODUCTION_BASELINE.md`. No IDOR
or cross-resident data-isolation defect found in the ViewSets spot-checked (`RotationAssignment`,
`LogbookEntry` academics variant, `DeputationPosting`, `SubmissionRequirementTemplate`,
`SupervisorReviewQueueItem`) — every one filters by `request.user`'s role via `get_queryset()`,
consistent with `11_SECURITY_RBAC_REVIEW.md`'s "PASS" verdict on this category.

**Note per user instruction (2026-09-09): no one outside this session has repo access; credential
rotation for XC-1 is deferred to later, but is confirmed as item 1 in the upcoming fixation/remediation
plan — see `15_BUILD_ORDER_AND_ROADMAP.md` BUILD WAVE 0.**

## Backend

Scope: `backend/sims/` (installed apps only). Verified live 2026-09-09 via a throwaway local venv
(see `10_TEST_AND_CI_BASELINE.md` "## Backend Tests & CI" for exact commands/output).

| ID | Priority | Finding | Evidence |
|---|---|---|---|
| BE-1 | **P2** | Root `CLAUDE.md` warns that "two independent `ResidentTrainingRecord` models exist" (`academics` and `training`). This is **stale** — the duplicate was consolidated and dropped via migration; only `training.ResidentTrainingRecord` exists today, `academics` models hold FK references to it, not a copy. Following the doc as written risks reintroducing the exact problem it warns about, or wasted effort. Recommend updating `CLAUDE.md` to remove/correct this paragraph. | `sims/academics/migrations/0006_...` → `0010_link_training_record_new.py` → `0011_finalize_training_record_retarget.py` → `0012_drop_academics_resident_training_record.py`; `grep -n "class ResidentTrainingRecord" sims/academics/models.py sims/training/models.py` → only `training/models.py:111` |
| BE-2 | **P1** | A genuine, currently-live, *unresolved* duplicate of the same shape CLAUDE.md warns about for `ResidentTrainingRecord`: two active `LogbookEntry` models (`academics.LogbookEntry`, the real product surface used by web+Android; `training.LogbookEntry`, used only by milestone-threshold computation, bulk import, and one legacy dashboard counter). They never sync. Concretely: real, verified logbook entries never count toward `_evaluate_logbook_thresholds()` milestone eligibility; a legacy supervisor "pending review" badge (`get_documents_pending_count()`, still live at `/users/supervisor-dashboard/`) always undercounts because it queries the wrong model, silently (bare `except Exception: pass`); bulk-imported entries land in a model the resident's actual logbook page never reads. Full detail and exact line numbers in `03_CANONICAL_DOMAIN_MODEL_MAP.md`. This is the single highest-value backend finding of this pass — recommend resolving before further logbook/milestone feature work (see Build Wave 0/1 in `15_BUILD_ORDER_AND_ROADMAP.md`). | `sims/academics/models.py:563`; `sims/training/models.py:986`; `sims/training/views.py:2004` (`_evaluate_logbook_thresholds`); `sims/bulk/services.py:287-290`; `sims/users/models.py:302-364` (`get_documents_pending_count`, both branches wrapped in bare `except Exception: pass`); `sims/users/views.py:96` (live call site) |
| BE-3 | **P3** | Legacy server-rendered Django-template dashboard views (`/users/dashboard/`, `/users/supervisor-dashboard/`, `/users/admin-dashboard/`, `/users/resident-dashboard/`, mounted `sims_project/urls.py:160`) predate the Next.js frontend, are not linked from it, but remain live, reachable, and (per BE-2) currently produce misleading output if reached directly. No active harm (nothing links to them) but worth an explicit retire-or-keep decision rather than indefinite dual-maintenance. | `sims/users/urls.py`; `sims_project/urls.py:160` |
| BE-4 | **P2** | `create_disaster_recovery_backup()` doesn't ensure its target `backend/backups/` directory exists before writing a zip file to it — fails with `FileNotFoundError` in any environment where that directory hasn't been separately pre-created (reproduced live in this pass's test run). Unconfirmed whether prod/CI happen to have the directory pre-created some other way; if not, the real DR mechanism, not just its test, would fail identically during an actual disaster. | `sims/backup_center/services.py:429`; reproduced via `pytest sims/backup_center/tests.py::TestBackupCenterServices::test_create_disaster_backup` |
| BE-5 | **P3** | Root `CLAUDE.md` describes `backend/pytest.ini` as carrying no coverage enforcement (only `pyproject.toml` does, at 80%). Current `pytest.ini` actually already enforces coverage itself, via `addopts`, at `--cov-fail-under=70`. Minor doc-accuracy correction. | `backend/pytest.ini` `addopts` line, read directly |

No P0 backend findings. `python manage.py check` and `makemigrations --check --dry-run` both clean;
1190/1191 backend tests pass (99.9%), the one failure is BE-4 above, environment-dependent rather
than a logic bug in the tested code path itself.
