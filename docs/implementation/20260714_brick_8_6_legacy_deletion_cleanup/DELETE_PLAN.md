# Delete Plan

1. Collapse duplicate frontend dashboard routes to redirect-only pages.
2. Delete deprecated frontend helper files with no active canonical imports.
3. Replace stale dashboard tests that still referenced deleted helper surfaces.
4. Remove active runtime hooks and admin exposure for `SupervisorResidentLink`.
5. Update canonical docs, route audit, and delete-candidate registry.
6. Run frontend, backend, and gate verification.
