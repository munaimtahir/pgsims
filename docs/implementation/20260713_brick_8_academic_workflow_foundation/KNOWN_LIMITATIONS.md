# Brick 8 Known Limitations

- Seed command currently populates minimum pilot periods only.
- Existing pre-Brick-8 `training` workflow surfaces still exist in the repo and remain outside the new canonical Brick 8 contract.
- Brick 8 intentionally does not implement evaluation submissions, scoring, logbook entries, or resident rotation scheduling.
